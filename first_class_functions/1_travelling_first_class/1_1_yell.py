"""
The examples will build on top of one another, so you might want to read them
 in sequence and even to try out some of them in a Python interpreter session
  as you go along.
"""


def yell(text):
    return text.upper() + '!'

print(yell('hello'))
# outputs
# 'HELLO!'
